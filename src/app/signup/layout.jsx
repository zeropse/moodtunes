export const metadata = {
  title: "Sign Up",
  description:
    "Create a new MoodTunes account to start discovering music based on your mood.",
};

export default function SignupLayout({ children }) {
  return <>{children}</>;
}
