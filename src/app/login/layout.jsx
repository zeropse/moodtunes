export const metadata = {
  title: "Login",
  description:
    "Login to your MoodTunes account to access your personalized mood-based playlists.",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
